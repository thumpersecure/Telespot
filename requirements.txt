# Telespot Requirements
# Install with: pip install -r requirements.txt

# Async HTTP engine (all speed modes run on httpx)
httpx>=0.28.1

# Brotli decoder. Without it the tool no longer advertises "br" compression,
# because servers would otherwise reply with bodies httpx cannot decode.
brotli>=1.1.0
